/*** Super Class for a component based on MVC approach */
export class Model {
    constructor() { /* some base props */ }

    /** @abstract */
    setData(data) { /*use data and set it to init your props*/ }
}